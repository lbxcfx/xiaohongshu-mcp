# 开发流程

## 1. 进入 WSL
```bash
wsl -d Ubuntu
cd /mnt/f/xiaohongshu-mcp
```

一键启动前后端：
```bash
bash /mnt/f/xiaohongshu-mcp/start-dev.sh
```

## 2. 后端启动
当前验证可用的启动命令：
```bash
cd /mnt/f/xiaohongshu-mcp
env ROD_BROWSER_BIN=/home/lbx/chrome-for-testing/chrome-linux64/chrome /usr/local/go/bin/go run .
```

如果希望退出当前 shell 后继续保活，使用 `tmux`：
```bash
cd /mnt/f/xiaohongshu-mcp
tmux kill-session -t xhs_backend 2>/dev/null || true
tmux new-session -d -s xhs_backend 'bash'
tmux send-keys -t xhs_backend 'cd /mnt/f/xiaohongshu-mcp && env ROD_BROWSER_BIN=/home/lbx/chrome-for-testing/chrome-linux64/chrome /usr/local/go/bin/go run . > /tmp/xhs-backend.log 2>&1 &' Enter
```

查看后端日志：
```bash
tail -f /tmp/xhs-backend.log
```

说明：
- 后端地址：`http://localhost:18060`
- `ROD_BROWSER_BIN` 必须指向 WSL 里的 Linux Chrome

## 3. 前端启动
当前验证可用的启动命令：
```bash
cd /mnt/f/xiaohongshu-mcp/ai-marketing
/mnt/f/xiaohongshu-mcp/ai-marketing/node_modules/.bin/tsx /mnt/f/xiaohongshu-mcp/ai-marketing/server/_core/index.ts
```

如果希望退出当前 shell 后继续保活，使用 `tmux`：
```bash
cd /mnt/f/xiaohongshu-mcp/ai-marketing
tmux kill-session -t ai_marketing_frontend 2>/dev/null || true
tmux new-session -d -s ai_marketing_frontend 'bash'
tmux send-keys -t ai_marketing_frontend 'cd /mnt/f/xiaohongshu-mcp/ai-marketing && env NODE_ENV=development /mnt/f/xiaohongshu-mcp/ai-marketing/node_modules/.bin/tsx /mnt/f/xiaohongshu-mcp/ai-marketing/server/_core/index.ts > /tmp/ai-marketing-frontend.log 2>&1 &' Enter
```

查看前端日志：
```bash
tail -f /tmp/ai-marketing-frontend.log
```

说明：
- 前端地址：`http://localhost:3000`
- 如果 `3000` 被占用，服务会自动切到 `3001`

## 4. 当前验证通过的前后端组合
前端：
```bash
cd /mnt/f/xiaohongshu-mcp/ai-marketing
tmux kill-session -t ai_marketing_frontend 2>/dev/null || true
tmux new-session -d -s ai_marketing_frontend 'bash'
tmux send-keys -t ai_marketing_frontend 'cd /mnt/f/xiaohongshu-mcp/ai-marketing && env NODE_ENV=development /mnt/f/xiaohongshu-mcp/ai-marketing/node_modules/.bin/tsx /mnt/f/xiaohongshu-mcp/ai-marketing/server/_core/index.ts > /tmp/ai-marketing-frontend.log 2>&1 &' Enter
```

后端：
```bash
cd /mnt/f/xiaohongshu-mcp
tmux kill-session -t xhs_backend 2>/dev/null || true
tmux new-session -d -s xhs_backend 'bash'
tmux send-keys -t xhs_backend 'cd /mnt/f/xiaohongshu-mcp && env ROD_BROWSER_BIN=/home/lbx/chrome-for-testing/chrome-linux64/chrome /usr/local/go/bin/go run . > /tmp/xhs-backend.log 2>&1 &' Enter
```

验证端口：
```bash
ss -ltnp | grep 3000
ss -ltnp | grep 18060
```

当前正确结果应类似：
- 前端：`*:3000`
- 后端：`*:18060`

## 5. 登录验证
1. 打开 `http://localhost:3000`
2. 点击 `请登录`
3. 前端请求：`GET http://localhost:18060/api/v1/login/qrcode`
4. 手机扫码并确认
5. 前端轮询：`GET http://localhost:18060/api/v1/login/status`
6. 状态返回 `logged_in` 后，二维码弹窗自动关闭并跳转 `/dashboard`

手工验证状态接口：
```bash
curl http://localhost:18060/api/v1/login/status
```

登录成功后的预期返回：
```json
{"success":true,"data":{"status":"logged_in","is_logged_in":true,"username":"小红书用户"},"message":"检查登录状态成功"}
```

## 6. OAUTH_SERVER_URL 说明
- `OAUTH_SERVER_URL` 是原项目 OAuth 登录服务地址
- 主要给 `/api/oauth/callback`、换 token、取用户信息使用
- 当前如果只走小红书扫码登录，可以不配置
- 不配置时会打印日志，但不影响小红书扫码登录

## 7. 常用检查
前端类型检查：
```bash
cd /mnt/f/xiaohongshu-mcp/ai-marketing
./node_modules/.bin/tsc --noEmit
```

后端测试：
```bash
cd /mnt/f/xiaohongshu-mcp
/usr/local/go/bin/go test ./...
```

## 8. 常见问题
- `go: command not found`
  直接使用 `/usr/local/go/bin/go`

- 前端启动不了
  在 WSL 内重新安装依赖：
  ```bash
  cd /mnt/f/xiaohongshu-mcp/ai-marketing
  rm -rf node_modules
  pnpm install
  ```

- 获取二维码失败
  先确认后端是否在监听：
  ```bash
  ss -ltnp | grep 18060
  curl http://localhost:18060/api/v1/login/qrcode
  ```

- 扫码成功但不跳转
  先检查状态接口：
  ```bash
  curl http://localhost:18060/api/v1/login/status
  ```
  只要这里返回 `status=logged_in`，前端刷新后应自动关窗跳转

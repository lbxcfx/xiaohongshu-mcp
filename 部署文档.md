# 腾讯云部署文档

本文档用于将本系统部署到腾讯云 Linux 服务器。系统由两部分组成：

- `ai-marketing`：Node.js/React/tRPC Web 服务，默认端口 `3001`
- Go 小红书后端：通过 go-rod 控制 Chrome/Chromium，默认端口 `18060`

推荐部署架构：

```text
公网用户
  |
  | 80/443
  v
Nginx
  |
  | http://127.0.0.1:3001
  v
ai-marketing Node 服务
  |
  | XHS_API_URL=http://127.0.0.1:18060
  v
Go 小红书后端
  |
  v
Chrome/Chromium headless
```

只对公网开放 `80/443`。`3001` 和 `18060` 只监听本机或仅允许本机访问，不要直接暴露到公网。

## 1. 硬件配置建议

| 场景 | 配置 | 说明 |
| --- | --- | --- |
| 最小测试 | 2 核 4GB，80GB SSD，5Mbps | 能跑，但 Chrome + Node + Go 同时运行时余量不大 |
| 推荐生产起步 | 4 核 8GB，100-200GB SSD，10Mbps | 推荐配置，适合 1-3 个账号、低并发搜索/发布 |
| 多账号/频繁抓取 | 8 核 16GB，200GB SSD，20Mbps+ | 适合多个 Chrome 会话、视频下载、素材生成并发 |
| 多人正式商用 | 8 核 16GB 起，数据库/对象存储拆出去 | SQLite 改 MySQL，图片/视频放 COS |

建议先购买：

```text
腾讯云 CVM 或轻量应用服务器
Ubuntu 22.04/24.04
4 核 8GB
100GB SSD
10Mbps 带宽
```

如果只是个人使用，轻量应用服务器即可；如果后续要多人、多账号、可扩展，建议直接使用 CVM。

## 2. 安全组/防火墙

腾讯云安全组或轻量服务器防火墙建议只开放：

| 端口 | 来源 | 说明 |
| --- | --- | --- |
| 22 | 你的固定 IP | SSH 管理 |
| 80 | `0.0.0.0/0` | HTTP |
| 443 | `0.0.0.0/0` | HTTPS |

不要开放：

```text
3001
18060
```

这两个端口只给本机 Nginx 和内部服务访问。

## 3. 安装系统依赖

以下命令以 Ubuntu 为例。

```bash
sudo apt update
sudo apt install -y git curl nginx tmux ca-certificates fonts-noto-cjk fonts-noto-color-emoji
```

### 3.1 安装 Node.js 22

项目使用 `node:sqlite`，建议使用 Node.js 22.16 或更高版本。

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node -v
```

启用 pnpm：

```bash
corepack enable
corepack prepare pnpm@10.4.1 --activate
pnpm -v
```

### 3.2 安装 Go 1.24

项目 `go.mod` 要求：

```text
go 1.24.0
```

安装完成后确认：

```bash
go version
```

如果系统源没有 Go 1.24，建议从 Go 官方下载 Linux tar 包安装到 `/usr/local/go`，并配置：

```bash
echo 'export PATH=/usr/local/go/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
go version
```

### 3.3 安装 Chrome/Chromium

Go 后端需要通过 go-rod 控制浏览器，必须安装可用的 Chrome 或 Chromium。

安装后确认路径：

```bash
which google-chrome || which google-chrome-stable || which chromium || which chromium-browser
```

假设输出为：

```text
/usr/bin/google-chrome
```

后续环境变量使用：

```text
ROD_BROWSER_BIN=/usr/bin/google-chrome
```

如果服务器没有图形环境，仍然可以使用 headless 模式。

## 4. 部署代码

建议将代码放在 `/opt/xiaohongshu-mcp`。

```bash
sudo mkdir -p /opt/xiaohongshu-mcp
sudo chown -R $USER:$USER /opt/xiaohongshu-mcp
cd /opt
git clone <你的仓库地址> xiaohongshu-mcp
cd /opt/xiaohongshu-mcp
```

如果是上传代码包，也保持最终目录为：

```text
/opt/xiaohongshu-mcp
```

## 5. 安装依赖并编译

### 5.1 编译 ai-marketing

```bash
cd /opt/xiaohongshu-mcp/ai-marketing
pnpm install --frozen-lockfile
pnpm build
```

### 5.2 编译 Go 后端

```bash
cd /opt/xiaohongshu-mcp
mkdir -p bin
go build -o /opt/xiaohongshu-mcp/bin/xhs-backend .
```

## 6. 准备数据和日志目录

```bash
sudo mkdir -p /var/lib/xiaohongshu-mcp
sudo mkdir -p /var/log/xiaohongshu-mcp
sudo chown -R $USER:$USER /var/lib/xiaohongshu-mcp /var/log/xiaohongshu-mcp
```

默认建议：

- SQLite 数据库放在 `/var/lib/xiaohongshu-mcp/ai-marketing.sqlite`
- 日志放在 `/var/log/xiaohongshu-mcp`
- 小红书登录态、cookie 等运行时文件保留在项目 `.data` 下，注意备份

## 7. 配置环境变量

创建环境变量文件：

```bash
sudo nano /etc/xiaohongshu-mcp.env
```

示例内容：

```env
NODE_ENV=production
PORT=3001
XHS_API_URL=http://127.0.0.1:18060
SQLITE_DATABASE_PATH=/var/lib/xiaohongshu-mcp/ai-marketing.sqlite
ROD_BROWSER_BIN=/usr/bin/google-chrome
DISABLE_MANUS_RUNTIME=1

# 按实际功能填写
OPENAI_API_KEY=
ARK_API_KEY=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
PIXELLE_API_URL=
PIXELLE_API_KEY=
```

说明：

- `XHS_API_URL` 指向 Go 后端。
- `SQLITE_DATABASE_PATH` 指向生产 SQLite 数据库。
- `ROD_BROWSER_BIN` 必须是服务器上真实存在的 Chrome/Chromium 路径。
- 如果暂时只用小红书登录、搜索、选题，很多 AI 相关 key 可以先不配；调用对应功能时才会提示未配置。

## 8. systemd 服务

生产环境不建议使用 `start_dev.sh` 长期运行，因为它启动的是开发模式：

- Node 使用 `tsx watch`
- Go 使用 `go run`

生产应使用：

- `pnpm build && pnpm start`
- Go 编译后的二进制

### 8.1 Go 后端服务

创建服务文件：

```bash
sudo nano /etc/systemd/system/xhs-backend.service
```

内容：

```ini
[Unit]
Description=Xiaohongshu MCP Go Backend
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/xiaohongshu-mcp
EnvironmentFile=/etc/xiaohongshu-mcp.env
ExecStart=/opt/xiaohongshu-mcp/bin/xhs-backend -port :18060 -bin ${ROD_BROWSER_BIN}
Restart=always
RestartSec=5
StandardOutput=append:/var/log/xiaohongshu-mcp/xhs-backend.log
StandardError=append:/var/log/xiaohongshu-mcp/xhs-backend.log

[Install]
WantedBy=multi-user.target
```

### 8.2 ai-marketing 服务

创建服务文件：

```bash
sudo nano /etc/systemd/system/ai-marketing.service
```

内容：

```ini
[Unit]
Description=AI Marketing Web Service
After=network.target xhs-backend.service
Requires=xhs-backend.service

[Service]
Type=simple
WorkingDirectory=/opt/xiaohongshu-mcp/ai-marketing
EnvironmentFile=/etc/xiaohongshu-mcp.env
ExecStart=/usr/bin/pnpm start
Restart=always
RestartSec=5
StandardOutput=append:/var/log/xiaohongshu-mcp/ai-marketing.log
StandardError=append:/var/log/xiaohongshu-mcp/ai-marketing.log

[Install]
WantedBy=multi-user.target
```

### 8.3 启动服务

```bash
sudo systemctl daemon-reload
sudo systemctl enable xhs-backend ai-marketing
sudo systemctl start xhs-backend ai-marketing
sudo systemctl status xhs-backend
sudo systemctl status ai-marketing
```

本机检查：

```bash
curl http://127.0.0.1:18060/health
curl http://127.0.0.1:3001
```

查看日志：

```bash
tail -f /var/log/xiaohongshu-mcp/xhs-backend.log
tail -f /var/log/xiaohongshu-mcp/ai-marketing.log
```

## 9. Nginx 反向代理

创建 Nginx 配置：

```bash
sudo nano /etc/nginx/sites-available/xiaohongshu-mcp
```

内容：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    client_max_body_size 200m;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

将 `your-domain.com` 替换成实际域名。

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/xiaohongshu-mcp /etc/nginx/sites-enabled/xiaohongshu-mcp
sudo nginx -t
sudo systemctl reload nginx
```

如果暂时没有域名，也可以先用服务器公网 IP 访问，但正式使用建议配置域名和 HTTPS。

## 10. HTTPS

域名解析到服务器后，可以使用 Certbot 配置 HTTPS：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

检查自动续期：

```bash
sudo systemctl status certbot.timer
```

如果使用中国大陆服务器并通过域名公开访问，通常还需要完成备案。

## 11. 数据备份

至少备份以下内容：

```text
/var/lib/xiaohongshu-mcp/ai-marketing.sqlite
/opt/xiaohongshu-mcp/.data/xhs-accounts
/opt/xiaohongshu-mcp/ai-marketing/.data
```

建议开启腾讯云云硬盘快照，并增加 SQLite 备份任务。

SQLite 备份示例：

```bash
sudo apt install -y sqlite3
mkdir -p /var/lib/xiaohongshu-mcp/backups
sqlite3 /var/lib/xiaohongshu-mcp/ai-marketing.sqlite ".backup '/var/lib/xiaohongshu-mcp/backups/backup-$(date +%F).sqlite'"
```

可以加入 crontab，每天凌晨备份：

```bash
crontab -e
```

示例：

```cron
0 3 * * * sqlite3 /var/lib/xiaohongshu-mcp/ai-marketing.sqlite ".backup '/var/lib/xiaohongshu-mcp/backups/backup-$(date +\%F).sqlite'"
```

## 12. 更新发布流程

进入项目目录：

```bash
cd /opt/xiaohongshu-mcp
git pull
```

重新安装依赖并编译：

```bash
cd /opt/xiaohongshu-mcp/ai-marketing
pnpm install --frozen-lockfile
pnpm build

cd /opt/xiaohongshu-mcp
go build -o /opt/xiaohongshu-mcp/bin/xhs-backend .
```

重启服务：

```bash
sudo systemctl restart xhs-backend ai-marketing
sudo systemctl status xhs-backend
sudo systemctl status ai-marketing
```

## 13. 常见问题

### 13.1 Chrome 启动失败

检查：

```bash
echo $ROD_BROWSER_BIN
ls -l /usr/bin/google-chrome
```

也可以直接运行：

```bash
/usr/bin/google-chrome --headless --no-sandbox --disable-gpu --dump-dom https://www.xiaohongshu.com
```

如果缺少系统库，需要根据错误补安装依赖。

### 13.2 小红书登录二维码无法正常显示

检查 Go 后端日志：

```bash
tail -f /var/log/xiaohongshu-mcp/xhs-backend.log
```

确认：

- Chrome/Chromium 路径正确
- 服务器能访问小红书
- `ROD_BROWSER_BIN` 已配置
- Go 后端 `18060` 正常

### 13.3 前端提示 XHS API 请求失败

检查环境变量：

```bash
cat /etc/xiaohongshu-mcp.env
```

确认：

```text
XHS_API_URL=http://127.0.0.1:18060
```

检查后端：

```bash
curl http://127.0.0.1:18060/health
```

### 13.4 端口被占用

查看端口：

```bash
sudo ss -ltnp | grep -E ':3001|:18060'
```

重启服务：

```bash
sudo systemctl restart xhs-backend ai-marketing
```

### 13.5 SQLite 不适合多人并发

当前默认 SQLite 更适合单机、低并发使用。如果后续多人使用或数据增长较快，建议：

- 数据库迁移到腾讯云 MySQL
- 素材、封面、视频文件迁移到腾讯云 COS
- 后台任务和浏览器自动化拆成独立 Worker

## 14. 生产注意事项

- 不要将 `18060` 暴露公网。
- 不要用 `start_dev.sh` 作为生产守护进程。
- 小红书登录态、cookie、账号目录是敏感数据，必须限制服务器访问权限。
- 云服务器 IP 运行浏览器自动化可能触发平台风控，搜索/发布频率要控制。
- 建议对 `/var/lib/xiaohongshu-mcp` 和项目 `.data` 做定期快照。
- 国内大陆服务器使用域名公开访问时，通常需要备案。

